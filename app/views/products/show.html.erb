<div class="span6">
	<%= image_tag @product.image(:medium) %>
</div>

<div class="span6">
	<h1><%= @product.title %></h1>

	<%= simple_format @product.description %>

<div class="price">
	<% if @product.previous_price_in_pence %>
		Was <%= @product.previous_price_in_pence %>
	<% end %>

	<%= @product.price_in_pence %>

</div>

	<p>
		<%= render "addtobasket" %>



		
	</p>	